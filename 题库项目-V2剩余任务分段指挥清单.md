---
title: 题库项目-V2剩余任务分段指挥清单
aliases:
  - 知识航线V2分段指挥
tags:
  - 项目规划
  - 分段验收
date: 2026-08-29
---

# 知识航线 V2 剩余任务分段指挥清单

> 基线（2026-08-29 本地 PDF/PNG 提取补充后）：[[题库项目-V2优化总清单]] 共 206 项，已完成 179 项，未完成 27 项，完成率 86.9%。
>
> 执行规则：一次只执行一个任务包；先重新核对真实代码与对应未完成框，完成代码、正常/失败或恢复验证、相关测试与全端构建，再更新文档并立即暂停。依赖账号、线上环境、生产密钥或危险操作时只记录阻塞，不虚假勾选。

## 建议顺序

`第 4 包 -> 第 7 包 -> 第 1 包 -> 第 3 包 -> 第 5 包 -> 第 6 包 -> 第 10 包`

第 8、9、12 包涉及外部授权或生产环境，等明确授权后再做；第 11 包是可选产品功能。

## 第 1 包：本地 MinIO 对象存储闭环（12 项）

状态：本轮实现与本地验证完成，等待汇总后暂停。

范围：Multipart Upload/UploadSession、uploadId 与短期预签名 URL、私有 Bucket、对象存储合并、流式 SHA-256、multipart 清理、跨用户物理去重、ETag 边界、对象 key 最小权限、对象存储超时恢复，以及“为什么不用 PostgreSQL/Render 临时磁盘”的证据。

验收：仅使用本地 MinIO；正常上传与超时/权限/恢复场景可重复；不宣传为线上生产对象存储。CI 的 MinIO service container 归第 7 包。

完成记录（2026-08-29）：

- `server/test/object-storage.e2e-spec.ts`：6/6，通过 Multipart、预签名、远端分片恢复、私有 Bucket、跨用户 404、ETag/MD5 边界、响应流 SHA-256、ImportJob 对象流处理、错误凭据、对象存储断开、一次延迟超时后的同会话重试和过期 Abort 清理。
- `npm run test:minio`：本地 MinIO bucket/object 往返、缺失对象 404、匿名私有 Bucket 403 和清理通过。
- `server/npm run build`、`server/npx prisma validate` 和 `git diff --check`：通过。
- 默认 filesystem 兼容保留；`STORAGE_DRIVER=minio` 才启用对象存储。跨用户物理去重已经只在服务端完成并保留每用户逻辑所有权；生产 S3/R2 和线上故障演练仍未完成。

## 第 2 包：上传恢复与性能证据（11 项）

状态：技术实现与自动化证据已完成，已暂停；5 个星标理解项待用户本人验收。

范围：完整浏览器进程重启恢复、37% 精确暂停恢复、并发数 1/3/6/10 基线、Hash 策略权衡、8 MiB 分片权衡、Long Task 报告，以及 Promise 并发池、Web Worker、漏片/重复片、客户端 Hash 与秒传泄露的面试证据。

验收：保存可重复脚本和报告；数字来自真实运行，不宣称“零卡顿”或“高并发”。

完成记录（2026-08-29）：

- 已完成并勾选：完整 Chromium 进程恢复、37% 暂停恢复、并发 1/3/6/10 基线、Hash 策略权衡、8 MiB 权衡、Long Task 对比，共 6 项。
- 可重复入口：`npm run test:browser`、`npm run benchmark:upload`。
- 报告：`docs/evidence/upload-recovery-report.md`、`docs/evidence/upload-performance-report.md/json`。
- 验证：前端 74/74、后端单测 6/6、E2E 56/56、Worker 5/5、Playwright 6/6；Web、Electron renderer、NestJS、Worker 构建和 Prisma validate 均通过。
- 待本人验收且未勾选：Promise 并发池原因、Web Worker 职责、漏片/重复片证据链、客户端 Hash 信任边界、秒传泄露边界，共 5 项。
- 边界：数字来自本机环回和本地文件系统，不代表生产对象存储、公网吞吐、零卡顿或高并发。

## 第 3 包：导入审核与确定性提取（7 项）

状态：待指挥。

范围：补齐 `WAITING_REVIEW`、`PUBLISHING` 状态与迁移；图片/PDF 提取只进 Draft；JSON Schema 校验；保存 source/版本/promptVersion/置信度/审核人；分类、难度、答案由人工确认；语义去重只标候选；记录 token、延迟、失败和费用。

验收：不新增付费 AI；无可用本地模型/OCR 时将对应部分记录为阻塞，不能用假数据打勾。

本轮结果（2026-08-29）：已完成 6 项（审核状态机、Ajv JSON Schema、source/promptVersion/confidence/reviewerId 元数据、确定性建议人工确认、语义候选去重、PDF/PNG 提取只进 Draft）；PDF 文字层和预置英文语言包的 PNG OCR 均有正常 E2E，空文字层 PDF 失败路径也已验证。真实模型 token/费用仍未接入，因此指标清单项不勾选。确定性建议不是 AI/OCR，证据：`docs/evidence/import-review-and-frontend-quality-report.md`、`docs/evidence/document-extraction-report.md`。

## 第 4 包：Redis 锁边界 ADR（2 项）

状态：代码与自动化验证完成，待本人理解验收后暂停。

范围：核对是否存在数据库约束无法表达的跨进程共享资源；只有确有必要才实现分布式锁，并说明过期、续租、崩溃与 fencing token。

验收：允许结论为“不引入锁”，但必须用并发测试和 ADR 说明理由，不能为简历强行加锁。

完成记录（2026-08-29）：

- ADR：`docs/adr/0004-redis-lock-boundary.md`。
- 并发证据：`server/test/app.e2e-spec.ts` 的“并发创建同一 ImportJob”用例，12 路请求全部返回同一 `jobId`，数据库只保留 1 条任务。
- 完整验证：独立 PostgreSQL E2E 56/56；测试库为 `question_bank_test`，未连接正式数据库。
- 决策：当前不引入业务 Redis 分布式锁；BullMQ 内部 Redis 锁仅用于队列领取/stalled 恢复。
- 两项均为 `★` 面试重点，复选框在用户能说明数据库仲裁边界、锁的 TTL/续租/崩溃/fencing token 后再由用户本人勾选。

## 第 5 包：前端安全、性能与可访问性（5 项）

状态：待指挥。

范围：长列表真实卡顿阈值与虚拟列表决策、Markdown 编辑/预览、Markdown/HTML XSS 清洗、键盘/焦点/ARIA/对比度、中性主题与可切换个性主题。

验收：正常与恶意输入、键盘流程和响应式页面均验证；无真实卡顿则记录“不引入虚拟列表”的证据。

本轮结果（2026-08-29）：已完成 Markdown 编辑/预览、XSS 清洗和中性/专注主题切换 3 项；虚拟列表按当前约 225 条服务端分页且未达到卡顿阈值暂不引入；完整 WCAG 对比度审计仍待验证。证据：`docs/evidence/import-review-and-frontend-quality-report.md`。

## 第 6 包：错误追踪、基线与告警（5 项）

状态：待指挥。

范围：Web、Electron renderer、API 的 Sentry 或本地等价错误记录；commit/release 关联；基于真实基线的告警；PostgreSQL 不可用时 readiness/告警；前端异常关联路由、版本和 requestId。

验收：可先实现本地等价方案；真实 Sentry 与线上告警需要 DSN、项目和部署环境，缺少时明确阻塞。

## 第 7 包：CI 质量门禁与 service containers（3 项）

状态：本地门禁已完成，远程 CI 与 MinIO 容器证据阻塞，已暂停。

范围：Dependabot/CodeQL 或等价扫描的真实运行证据；PR format/lint/前后端单测；Actions 使用 PostgreSQL、Redis、MinIO service containers 跑集成测试。

验收：本地能验证的先验证；GitHub Actions 线上成功记录需要明确远程操作授权，不能把 YAML 存在当作运行成功。

完成记录（2026-08-29）：

- 已实现：根目录和 `server` 的 `format:check`、`lint`；CI 加入前端/后端测试；新增 `npm run ci:validate` 配置契约测试。
- 本地结果：前端 74/74、后端单测 6/6、根目录与后端格式检查通过、根目录与后端 lint 通过。
- 已配置：`.github/workflows/ci.yml` 声明 PostgreSQL 16、Redis 7、MinIO service containers；`.github/workflows/codeql.yml` 与 `.github/dependabot.yml` 保留并由契约测试核对。
- 证据：`docs/evidence/ci-quality-gate-report.md`。
- 阻塞：本机没有 Docker；没有远程 GitHub Actions/CodeQL/Dependabot 成功记录；应用尚未接入 MinIO，不能虚假勾选第 1、3 项。前端 `npm audit` 已复扫为 0 个漏洞，后端 Prisma 7 链路仍有 3 个 high，不能标记扫描“全绿”。第 2 项已具备本地实现和可重复验证，远程成功后再勾选。

## 第 8 包：Staging、Release 与 Electron 更新（5 项）

状态：外部环境阻塞。

范围：main 自动部署 staging 与 smoke test；tag 发布生产 API/Windows 安装包；Release SHA-256 与 changelog；GitHub Environments secrets；Electron 自动/差分更新和签名。

阻塞：需要 staging、GitHub/部署权限、生产 Secrets、代码签名证书和真实升级环境。执行前必须再次取得明确授权。

## 第 9 包：生产数据库迁移与回滚（3 项）

状态：外部环境阻塞。

范围：生产 `prisma migrate deploy`、失败部署与应用版本回滚演练；expand-contract 的测试库兼容性已完成，见 `server/scripts/expand-contract-smoke.mjs` 与 `docs/adr/0005-expand-contract-and-rollback.md`。

阻塞：必须先人工核对正式 Neon `_prisma_migrations` 与实际表结构并取得生产迁移确认；不得擅自 `resolve`、deploy 或修改正式数据。

## 第 10 包：用户本人理解与面试验收（7 项）

状态：需要用户参与。

范围：画完整数据流；解释业务目的；亲手写 10 到 30 行核心逻辑；观察页面/数据库前后变化；展示测试/压测/日志/故障演练；说明替代方案与权衡；说明仍未解决的问题。

验收：这些框只能由用户本人完成讲解或编码后勾选，Codex 不能代替理解验收。

## 第 11 包：可选学习产品增强（6 项）

状态：可选，低优先级。

范围：SM-2/遗忘曲线复习调度、AI 模拟面试 SSE、答案评分拆分、导入预览/逐项修正/整批回滚、行为漏斗埋点、隐私与遥测开关。

验收：不新增付费 AI；默认不采集答案正文；每个功能独立建模和验收，避免一次全部实现造成漂移。

## 第 12 包：版权与公开发布材料（1 项）

状态：需要用户确认来源。

范围：核查公开题目来源、许可和可分发范围，形成公开示例题库与引用说明。

阻塞：版权事实不能由代码推断，需要用户提供或确认题目来源。

## 指挥口令

- `执行第 N 包，完成后暂停。`
- `只诊断第 N 包，不改代码。`
- `继续第 N 包的下一小点，完成后暂停。`

若某包仍然过大，可继续拆成 `N.1、N.2、N.3`，但不跨包顺手修改。
