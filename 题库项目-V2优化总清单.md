---
title: 题库项目-V2优化总清单
aliases:
  - 知识航线优化清单
  - 知识航线V2 Backlog
tags:
  - 项目规划
  - 全栈
  - 大文件上传
  - 简历
  - Backlog
date: 2026-08-17
---
#  crtl+f 查找，c+k 展开，c+s+z 折叠
# 知识航线 V2 优化总清单

> 2026-08-29 本地验收摘要：本轮补齐审核导入页面、单题 Ajv PARTIAL 边界、MinIO multipart ETag 实验、跨用户内部物理去重、对象存储超时恢复、跨端隐私遥测设置、PostgreSQL readiness 本地告警、expand-contract 测试库兼容性、PDF/PNG 仅进 Draft，以及审核 Draft 的确定性建议与人工确认；并修复本地降级队列在 E2E 清理时未排空导致的连接池竞态。当前为 206 项中 179 项已完成、27 项未完成（86.9%）。真实模型费用、外部 Sentry、GitHub Actions 线上运行、staging 或生产环境任务继续保留未勾选。证据：`docs/evidence/v2-local-reliability-and-review-report.md`、`docs/evidence/object-storage-report.md`、`docs/evidence/import-review-and-frontend-quality-report.md` 与 `docs/evidence/document-extraction-report.md`。
>
> 后续不再全量连续推进，改为按 [[题库项目-V2剩余任务分段指挥清单]] 逐包执行；每包完成并验收后立即暂停。

> 本清单综合当前仓库、与 Gemini 的三份简历讨论记录、参考简历和上一版全栈路线图。`[x]` 代表仓库中已经存在并验证，`[ ]` 代表规划，未完成项不能提前写进简历。
>
> **面试重点标记：** 条目前的 `★` 表示面试官高概率追问，且能体现安全边界、数据一致性、并发控制、跨端架构或可靠性；优先准备其业务目的、数据流、失败场景、测试证据和替代方案。普通实现步骤、工具品牌、具体测试参数和低优先级产品功能不加星；`★` 不改变 `[x]/[ ]` 完成状态，也不代表必须马上实现。

> [!todo] 当前只做
> **第一阶段-1：为 Question 增加 normalizedTitle，并通过数据库唯一约束拦住并发重复。**
> 完成代码、迁移和并发测试后，才进入 User 数据模型。

## 四阶段执行总览

1. **第一阶段：系统正确且知道数据属于谁**
   数据库唯一约束、后端集成测试、用户认证、权限和个人学习数据云端化。
2. **第二阶段：大文件可靠上传并进入后台处理**
   对象存储、分片、Hash、并发池、断点续传、BullMQ 和 SSE。
3. **第三阶段：多人协作时不重复、不越权、不互相覆盖**
   幂等、乐观锁、审核状态机、版本审计和前端请求治理。
4. **第四阶段：证明系统在线上可靠**
   日志、监控、告警、故障演练、集成/E2E 测试和 CI/CD。

> 四个阶段必须顺序完成。可选 AI、复习算法和 Electron 自动更新放在四阶段之后。

---

## 第一阶段-1：数据正确性和测试地基

> 目的：后面功能越复杂，越不能建立在“数据库可能重复、接口没有集成测试”的基础上。

### 数据一致性

- [x] 为 Question 增加 `normalizedTitle` 字段。
- [x] ★ 为 `normalizedTitle` 建数据库唯一索引。

> 完成证据（2026-08-23）：新增可重复部署的回填迁移，先检查规范化标题冲突，再回填历史数据并将字段设为 `NOT NULL`；正式 Neon 库 225 条题目全部回填成功，`normalizedTitle` 非空 225 条、NULL 0 条，历史规范化重复 0 组；测试库和正式库均完成迁移记录。
- [x] ★ 使用 Prisma transaction 或数据库原生冲突处理完成批量导入。

> 完成证据（2026-08-17）：10 个相同标题的并发导入请求最终只写入 1 条，1 个请求返回 `importedCount: 1`，其余 9 个返回 `skippedCount: 1`；临时探针数据已清理。
- [x] ★ 并发提交相同题目时，一个成功，另一个得到明确重复结果。

> 完成证据（2026-08-28）：两个并发 `POST /questions` 请求由数据库唯一索引裁决，E2E 验证一个返回 `201`、另一个返回 `409 QUESTION_TITLE_DUPLICATE`，数据库最终只有 1 条记录。
- [x] 为题目增加 `version`，为后续乐观锁做准备。

> 完成证据（2026-08-17）：Neon 的 `Question.version` 为非空整数且默认值为 1，现有 225 道题全部完成初始化，无空值。

### API 基础规范

- [x] 统一成功/错误响应结构和错误码。

> 完成证据（2026-08-18）：全局 Interceptor 统一成功响应为 `{ success: true, data }`，全局 Exception Filter 统一错误响应为 `{ success: false, error: { code, message } }`；真实请求验证 `200`、DTO 校验 `400`、未知路由 `404` 和业务错误 `QUESTION_NOT_FOUND`，Web/Electron 共用的 `apiFetch` 保持原有调用兼容。
- [x] 增加 requestId，并在响应头、日志和错误信息中贯穿。

> 完成证据（2026-08-19）：Middleware 为每次请求生成 UUID 并写入 `X-Request-Id`，响应完成时日志记录同一 ID、route、status 和 duration；真实 `QUESTION_NOT_FOUND` 请求验证响应头、错误体 `error.requestId` 与服务端日志三处完全一致。
- [x] **08-20** 对分页、关键词、分类、标签建立统一查询 DTO。

> 完成证据（2026-08-20）：`FindQuestionsDto` 统一校验分页、关键词、分类和标签参数，非法分页及未知字段返回 `VALIDATION_ERROR`；真实请求验证无参数仍兼容返回 225 条、显式分页第二页从 ID 21 开始，关键词、分类、JSON 字符串标签及三者组合筛选均无越界结果。
- [x] ★ **08-20** 所有修改接口只接收白名单字段。

> 完成证据（2026-08-20）：全局 `ValidationPipe` 开启 `whitelist` 与 `forbidNonWhitelisted`，新增、修改和批量导入真实请求均拒绝客户端伪造的 `id`、`version`、`normalizedTitle`，返回 `VALIDATION_ERROR`；验证前后数据库题目数均为 225，无脏写。
- [x] ★ **08-20** 清空题库等危险操作移入管理员权限并增加二次确认。

> 完成证据（2026-08-25）：前端保留 `window.confirm` 和输入“清空题库”的二次确认；后端 `DELETE /questions` 使用 JWT `AuthGuard` 与 `RolesGuard`，未登录返回 401、LEARNER 返回 403、ADMIN 才能成功；后端 E2E 20/20、前端测试 39/39、生产构建均通过。

### 测试

- [x] ★ **08-20** 使用独立测试 PostgreSQL 编写 Questions API 集成测试。
- [x] **08-20** 覆盖正常导入、批内重复、库内重复和并发重复。
- [x] **08-20** 覆盖非法 schemaVersion、超长字段和错误 tags 类型。
- [x] **08-20** 测试后清理数据库，保证用例可以重复运行。

### 完成证据

- [x] **08-20** 测试报告能证明数据库唯一约束生效。
- [x] **08-20** 同时发送 10 个同标题请求，数据库最终只有 1 条。
- [x] **08-20** README 记录测试命令、测试数据库和结果。

---

## 第一阶段-2：多用户、鉴权和权限

### 用户与会话

- [x] **08-20** 新增 `User`、`RefreshSession` 数据模型。

> 完成证据（2026-08-24）：正式 Neon 数据库已成功应用 `20260823120000_add_refresh_session_model`；已查询确认 `User`、`RefreshSession` 表存在，`RefreshSession_userId_fkey` 外键存在，`_prisma_migrations.finished_at` 有完成记录。
- [x] ★ **08-20** 注册时规范化邮箱并使用 Argon2/bcrypt 保存密码哈希。

> 完成证据（2026-08-24）：注册 Service 统一执行 `trim().toLowerCase()` 和 `bcrypt.hash(password, 12)`；E2E 验证响应不返回密码、数据库不保存明文，并使用 `bcrypt.compare` 验证原密码；大小写重复邮箱返回 409。
- [x] ★ **08-20** Access Token 短期有效，Refresh Session 可撤销、可轮换。

> 完成证据（2026-08-24）：登录签发 15 分钟 JWT；RefreshSession 保存 Refresh Token 的 SHA-256 哈希并设置 30 天过期时间；刷新使用事务撤销旧 Session 并创建新 Session；退出会将当前 Session 标记为 revoked；E2E 覆盖登录、轮换、旧令牌失效和退出后不可刷新，全部通过。
- [x] ★ **08-20** Web 的 refresh token 使用 HttpOnly、Secure Cookie。

> 完成证据（2026-08-24）：登录只在 JSON 返回 Access Token，Refresh Token 写入 `refresh_token` Cookie；Cookie 设置 `HttpOnly`、生产环境 `Secure`、`SameSite=Lax`、`Path=/auth`；刷新从 Cookie 读取并轮换，退出清除 Cookie；开启 CORS `credentials` 和 cookie-parser；E2E 验证 Set-Cookie 安全属性及 JSON 不泄露 Refresh Token。
- [x] **08-20** 登录、刷新、退出和主动踢下线都有测试。

> 完成证据（2026-08-25）：E2E 覆盖登录签发、Refresh Token 轮换、当前设备退出和 `/auth/logout-all` 主动退出所有设备；两个独立 Session 被同一用户的退出所有设备操作一起撤销，后续刷新均返回 401。
- [x] ★ **08-21** Electron 使用主进程和 `safeStorage` 保存长期令牌，不暴露给渲染层。

> 完成证据（2026-08-25）：主进程使用 `safeStorage.encryptString/decryptString` 保存加密的 `refresh-token.bin`；登录、刷新、退出由主进程通过 IPC 编排，preload 只暴露 `auth.login/refresh/logout`，不暴露令牌读取方法；token vault 测试覆盖加密存储、解密读取、清除和安全存储不可用时拒绝明文回退；前端测试 30/30、Web 与 Electron 构建均通过。

### Token 并发刷新

- [x] ★ **08-21** `apiFetch` 增加认证头、超时、统一错误解析。

> 完成证据（2026-08-25）：`apiFetch` 只在内存保存 Access Token，Web/Electron 请求统一补 `Authorization`；Web 默认携带 HttpOnly Cookie；默认 10 秒超时并支持 `AbortSignal`；后端错误统一转换为带 `status`、`code`、`requestId` 的 `ApiError`；请求层测试覆盖认证头、Cookie、403 错误解析和超时，前端测试 34/34、Web/Electron 构建通过。
- [x] ★ **08-21** 多个请求同时遇到 401 时只允许一个刷新请求执行。
- [x] **08-21** 其余请求等待同一个 refresh Promise，成功后统一重放。

> 完成证据（2026-08-25）：请求层使用共享 `refreshPromise` 实现 single-flight；3 个并发 401 请求只触发 1 次刷新，刷新成功后各自只重放 1 次，测试通过。
- [x] ★ **08-21** 刷新失败时只退出一次，并拒绝所有等待请求。

> 完成证据（2026-08-25）：刷新失败时通过共享 `logoutPromise` 只调用一次 Web/Electron 退出流程并清理内存 Access Token；所有等待请求共享原始刷新错误并被拒绝；3 个并发失败请求的刷新调用为 1 次、退出调用为 1 次，测试通过。
- [x] ★ **08-21** 使用 `AbortController` 在退出登录或切换页面时取消无效请求。

> 完成证据（2026-08-26）：会话级 `AbortController` 在退出时取消旧账号请求并重新创建；路由 `beforeEach` 在切页时取消旧页面请求；请求层区分 `REQUEST_ABORTED` 和 `TIMEOUT`，测试覆盖退出取消、切页取消及取消后新请求仍携带 Access Token，前端测试 37/37、Web/Electron 构建通过。

### RBAC 与对象级权限

- [x] **08-21** 角色：LEARNER、EDITOR、ADMIN。
- [x] ★ **08-21** 前端根据权限生成菜单/路由，并用指令控制按钮显示。
- [x] ★ **08-21** 后端使用 AuthGuard + RolesGuard 校验角色。
- [x] ★ **08-21** Service 层校验数据所有权，不能信任请求体中的 userId。
- [x] ★ **08-21** A 用户不能读取、修改或删除 B 用户的练习记录。
- [x] **08-21** 覆盖 401、403 和越权访问测试。

### 完成证据

- [x] **08-21** 两个测试账号的数据隔离演示。
- [x] **08-21** 并发发出 10 个过期请求，只出现 1 次 refresh 网络请求。
- [x] **08-21** 直接调用隐藏按钮对应 API 仍然返回 403。

> 完成证据（2026-08-27）：后端 E2E 在隔离的 `TEST_DATABASE_URL` 中注册 A、B 两个账号，验证 A 可以读取自己的练习记录，但读取、修改、删除 B 的记录均返回 404，且数据库中的 B 记录未被修改或删除；未携带 Token 访问受保护接口返回 401。管理员专属“清空题库”按钮在前端由权限指令隐藏，LEARNER 绕过页面直接调用其 `DELETE /questions` 接口仍返回 403，ADMIN 调用成功。请求层使用 10 个并发过期 Access Token 请求验证 single-flight，10 个请求只产生 1 次 refresh 网络请求，每个原请求只重放 1 次。验收结果：前端测试 39/39、后端单测 3/3、后端 E2E 20/20，Web、Electron、NestJS 构建全部通过。

---

## 第一阶段-3：个人学习数据云端化

- [x] ★ **08-21** PracticeRecord 增加 userId 外键。
- [x] **08-21** 新增 Favorite，并建立 `(userId, questionId)` 联合唯一索引。
- [x] **08-21** 新增个人 Note 或错题备注。
- [x] ★ **08-21** 每次练习由客户端生成 `clientRequestId`。
- [x] ★ **08-21** 数据库建立 `(userId, clientRequestId)` 唯一约束。
- [x] ★ **08-21** 用户连续点击提交或网络重试只产生一条记录。
- [x] **08-21** 第一次登录提供本地数据迁移预览，而不是静默覆盖云端。
- [x] ★ **08-21** 网页和 Electron 登录同一账号后看到相同进度。
  完成证据（2026-08-29）：Playwright 先在真实 Chromium 登录并写入练习记录，再启动真实 Electron 登录同一账号；Electron 的复习记录显示 Web 写入内容。关闭整个桌面进程并使用同一 userData 目录重启后，加密 Refresh Token 恢复会话且仍能读取该记录。测试同时验证 Electron API 测试域名必须显式加入白名单，默认生产白名单未放宽。
- [x] **08-21** 未登录时保留本地访客模式，并明确其不会自动跨设备同步。
  完成证据（2026-08-29）：未登录时 `practice` Store 只写入 localStorage，`App.vue` 明确显示“只保存在本机，不会自动同步到其他设备”；前端单测验证访客保存不会调用云端 API，70/70 通过，Web/Electron 构建通过。

### 完成证据

- [x] **08-21** 同一个请求重放 10 次，数据库只新增 1 条。

> 完成证据（2026-08-29）：后端 E2E 并发重放同一 `clientRequestId` 10 次，10 个响应指向同一记录 ID，数据库计数为 1；后端 E2E 29/29 通过。
- [x] **08-21** Web 作答后，Electron 能读取该记录。
  完成证据（2026-08-29）：新增 Playwright 跨端用例，用 Web UI 完成作答和自评后，在 Electron UI 的同一道题“复习记录”中读取同一答案；后端再用该用户 Token 查询并确认数据库存在该记录。测试还发现并修复登录后练习记录加载被路由导航取消的竞态，70/70 前端单测和跨端用例通过。
- [x] **08-21** 不同账号的统计结果互不影响。
  完成证据（2026-08-29）：后端 E2E 为 A、B 两个账号分别写入练习记录，`GET /practice-records/summary` 只返回当前 Token 对应用户的数量和最新时间；A 有 2 条时 B 返回 0 条，且跨用户读取/修改/删除均被拒绝。62/62 E2E 通过。

> PracticeRecord 外键完成证据（2026-08-27）：练习记录保存 `userId`，并通过数据库外键关联真实的 `User.id`。用不存在的用户编号尝试直接写入数据库时，PostgreSQL 拒绝保存；Prisma E2E 测试通过，正式数据库迁移状态为最新。这里证明的是“练习记录有合法的用户归属”，还不代表后面的重复提交、跨设备同步和统计隔离已经完成。

> Favorite 和 Note 完成证据（2026-08-27）：Favorite 表保存“哪个用户收藏了哪道题”，同一个用户重复点击收藏不会产生第二条记录；A、B 两个用户的收藏互相看不到。Note 表保存“哪个用户对哪道题写的个人备注”，同一用户再次保存会更新原备注，不会新增重复记录；不同用户可以分别保存自己的备注。两张表都关联真实用户和题目，接口从登录 Token 获取用户身份。后端 E2E 共 23/23 通过，Web、Electron、NestJS 构建全部通过。这里证明的是收藏和个人备注的后端基础能力，前端按钮和跨设备读取还未接入。

> clientRequestId 客户端生成完成证据（2026-08-28）：PracticeSession 在一次作答第一次保存时生成 UUID，切换题目或模式时清空旧编号；practice Store 接收并保存该编号，测试断言编号不会丢失。前端测试 39/39、Web 构建通过。这里证明的是客户端能为一次作答稳定编号，服务端字段、用户联合唯一约束和重复请求只写一条将在后续清单项完成。

> PracticeRecord 联合唯一约束完成证据（2026-08-28）：数据库迁移为 `PracticeRecord.clientRequestId` 回填历史记录后设置为必填，并建立 `(userId, clientRequestId)` 唯一索引；同一用户重复写入相同请求编号时，PostgreSQL/Prisma 返回 `P2002`，数据库最终只保留一条记录。

> 练习提交幂等完成证据（2026-08-28）：新增受登录保护的 `POST /practice-records`，用户身份只从 Access Token 获取；Service 第一次创建记录，遇到唯一索引冲突 `P2002` 时查询并返回原记录。E2E 使用同一账号和同一个 `clientRequestId` 并发重放 10 次，10 次响应返回同一个记录 ID，数据库计数为 1。验收结果：后端 E2E `25/25`、后端单测 `3/3`、NestJS 构建通过；前端测试 `39/39` 和 Web 构建沿用本轮此前通过结果。

> 第一次登录迁移预览完成证据（2026-08-28）：登录恢复后自动显示本机与当前账号云端的练习记录数量，由用户选择“迁移到云端”“保留本地”或“稍后处理”；决定按用户分别保存，也可以从“学习数据备份与迁移”手动重新打开。迁移只追加，保留本地副本，不清空或覆盖云端旧记录；旧本地记录会获得稳定的 `clientRequestId`，失败重试不会重复写入。真实页面验收确认：“稍后处理”后刷新仍会再次询问；选择“保留本地”后，本机仍为 1 条、云端仍为 0 条。E2E 还验证了 A/B 账号云端预览隔离、历史练习时间保留和迁移后云端旧记录仍存在。验收结果：前端测试 `42/42`、后端单测 `3/3`、后端 E2E `26/26`，Web、Electron 和 NestJS 构建全部通过。


---

## 第二阶段-1：大文件可靠传输

### 业务范围

支持上传用于题库导入的：

- 大批面试截图 ZIP。
- PDF 面经或学习资料。
- 大型 JSON 题库。
- 后续 OCR/AI 处理需要的原始文件。

不做与题库无关的通用网盘，避免项目失焦。

### 为什么不能直接传给 Render 后端

Render 实例的本地磁盘不适合保存长期文件，实例重启或重新部署后临时文件可能丢失；大文件全部经过 API 也会占用服务器带宽、内存和连接时间。因此生产方案采用：

- 本地开发：MinIO。
- 线上环境：Cloudflare R2 或 AWS S3。
- ★ PostgreSQL：只保存文件元数据、归属和任务状态，不保存文件二进制。
- ★ 客户端：通过短期预签名 URL 直接上传分片到对象存储。

### 数据模型

- [x] **08-22** `UploadSession`：userId、objectKey、fileName、size、mime、sha256、status、partSize、expiresAt。
- [x] **08-22** `UploadPart`：sessionId、partNumber、etag、size、checksum、uploadedAt。
- [x] **08-22** `FileObject`：objectKey、ownerId、sha256、size、verifiedAt、scanStatus。
- [x] ★ **08-22** UploadSession 状态：CREATED、UPLOADING、UPLOADED、VERIFYING、COMPLETED、FAILED、EXPIRED。

> 完成证据（2026-08-29）：新增 Prisma 模型和迁移；本地上传服务按用户保存会话/分片/文件元数据，数据库唯一约束和外键已生效。
> 对象存储补充证据（2026-08-29）：`UploadSession`/`FileObject` 增加 `bucket`、`uploadId`、`storageBackend`；默认 filesystem 保持兼容，MinIO 模式记录对象存储归属。

### 初始化上传

- [x] **08-22** `POST /uploads/initiate` 接收文件名、大小、类型和可选指纹。
- [x] ★ **08-22** 服务端检查登录、配额、文件类型、大小上限和用户归属。
  完成证据（2026-08-29）：`UploadsService.initiate` 校验登录、MIME 白名单、单文件 500 MiB、每用户 100 个文件和 2 GiB 已占用/进行中空间，并按 ownerId 查询会话与 FileObject；E2E 验证非法 MIME、跨用户会话、累计字节超限和校验失败路径。配额为单进程/单库本地演示，生产多实例共享配额仍需集中式计数。
- [x] ★ **08-22** 创建对象存储 Multipart Upload 和 UploadSession。
  完成证据（2026-08-29）：`ObjectStorageService` 在 `STORAGE_DRIVER=minio/s3` 下创建真实 Multipart Upload，并将 bucket/uploadId 写入 UploadSession；本地 MinIO NestJS E2E 通过。
- [x] **08-22** 返回 uploadId、sessionId、partSize 和短期预签名 URL。
  完成证据（2026-08-29）：`POST /uploads/initiate` 在对象存储模式返回 uploadId、sessionId、8 MiB partSize 和每个分片的短期预签名 URL；独立 presign 接口做所有权与分片范围校验，MinIO E2E 验证 URL 与过期上限。
- [x] **08-22** 分片大小先固定为 8 MiB，后续按文件大小和 10000 分片上限动态调整。

> 完成证据（2026-08-29）：`UploadsService` 固定 `partSize=8 MiB`，E2E 上传两片并在数据库中查询到已完成分片。

> 本地验证证据（2026-08-29）：后端 E2E 33/33；登录用户完成两片上传、状态查询、合并和 SHA-256 校验，另一用户读取会话返回 404，错误 Hash 返回 400 并将会话标记为 FAILED，不在白名单中的 MIME 返回 400。线上对象存储和预签名 URL 尚未验证。

### 前端切片与 Hash

- [x] ★ **08-22** 使用 `Blob.slice()` 按 partSize 切片，不把整个文件读入内存。
  完成证据（2026-08-29）：`uploadFile` 使用 `Blob.slice()` 逐片上传，`UploadPanel` 已接入题库管理页；前端测试覆盖固定大小切片和完整上传流程，Web/Electron 构建通过。
- [x] ★ **08-22** 使用 Web Worker 增量计算 SHA-256，主线程只接收进度和结果。
  完成证据（2026-08-29）：`src/workers/sha256.worker.js` 使用 `@noble/hashes/sha2.js` 按 8 MiB 增量 Hash，主线程只接收 progress/done；Chromium Playwright 对真实大文件完成 Worker Hash、上传和服务端摘要校验，Web/Electron 构建均产出独立 Worker chunk。
- [x] **08-22** Hash 计算展示独立进度，不能把它伪装成上传进度。
  完成证据（2026-08-29）：`UploadPanel` 将 hashing、uploading、verifying、processing 分成独立状态和文案；状态机单测覆盖合法/非法迁移，Chromium 上传闭环验证 Hash 完成后才进入 XHR 字节上传。
- [x] **08-22** 记录“先完整 Hash 再秒传”和“边上传边 Hash”的性能权衡。
  完成证据（2026-08-29）：`npm run benchmark:upload` 对固定 96 MiB 文件验证两种策略摘要一致；本机环回条件下先完整 Worker Hash 再上传为 1062.4 ms，边 Hash 边上传为 788.6 ms。报告记录当前选择前者是为了上传前秒传判断和稳定恢复键，同时明确首请求更晚、数字不能外推生产环境。
- [x] **08-22** 不把对象存储 multipart ETag 当成整个文件的 MD5。
  完成证据（2026-08-29）：本地 MinIO 两片 Multipart 完成后读取对象 ETag，与完整文件 MD5 实际比较不相等；测试保留 ETag 作为对象存储完成凭证，不将其当作文件摘要。

### Promise 并发池

- [x] ★ **08-22** 手写 `runWithConcurrency(tasks, limit)`，默认同时上传 4 到 6 个分片。
- [x] **08-22** 一个分片结束后再从等待队列取下一个，不使用全量 `Promise.all`。
- [x] ★ **08-22** 单个分片失败不立即取消全部，按错误类型决定重试。
- [x] **08-22** 401、403、413、格式错误等永久错误不重试。
- [x] ★ **08-22** 超时、网络中断、部分 5xx 使用带抖动的指数退避。
- [x] **08-22** 设置每片最大重试次数，最终失败进入明确状态。

> 完成证据（2026-08-29）：`uploadClient.js` 的并发池限制同时任务数，`withUploadRetry` 只对瞬时错误按指数退避重试；单测验证并发峰值、失败后成功和 403 永久失败不重试。

### 上传进度

- [x] **08-22** 普通 API 继续使用现有 `apiFetch`。
- [x] **08-22** 总进度按每个分片已上传字节求和，而不是按“完成分片数量”粗略计算。
- [x] **08-22** 浏览器分片上传单独使用支持 upload progress 的 XHR Transport。
  完成证据（2026-08-29）：Web Transport 使用 `XMLHttpRequest.upload.onprogress` 汇总真实在途字节；Playwright 在首片尚未完成时观察到 `8.0 MB` 字节进度，并验证暂停会中止在途请求、恢复后不会重传已完成分片。
- [x] **08-22** UI 显示 Hash、上传、校验、解析四个阶段，避免一个进度条含义混乱。
  完成证据（2026-08-29）：UploadPanel 分别呈现 hashing、uploading、verifying 和 ImportJob processing；前端状态机单测覆盖完整链路和失败重试，Playwright 验证上传、后台导入和最终 1/1 进度。
- [x] **08-22** Electron 可先复用浏览器 Transport；若受 CORS 限制，再实现主进程 Transport 和 IPC 进度事件。
  完成证据（2026-08-29）：Electron 使用独立 `electron-ipc` Upload Transport；preload 仅暴露 `upload.part/abort`，主进程校验 API origin、请求头白名单和 16 MiB 单片上限，以 256 KiB 流式发送并按 `requestId` 回传真实字节进度。前端单测覆盖认证头、ArrayBuffer、进度和 Abort 适配；Playwright 真实上传 40 MiB 文件、观察已完成分片、暂停并关闭整个 Electron 进程，复用同一 userData 重启后确认同一 sessionId/已完成分片并补传成功，最终 ImportJob 为 SUCCEEDED 且数据库存在导入题目。

### 完成、校验与清理

- [x] ★ **08-22** `POST /uploads/:id/complete` 只接受该用户会话的 partNumber/etag。
- [x] ★ **08-22** 服务端确认分片连续、总大小正确后通知对象存储完成合并。
  完成证据（2026-08-29）：MinIO 模式先核对连续 partNumber/总字节，再调用 `CompleteMultipartUpload`；随后下载对象做大小、Hash、MIME 和 ZIP 安全校验，MinIO E2E 通过。
- [x] ★ **08-22** 服务端根据真实文件内容识别 MIME，不只信任扩展名。
  完成证据（2026-08-29）：合并后的本地文件先读取真实文件头，再与声明 MIME 比对；JSON、ZIP、PDF、PNG、JPEG、WEBP 均有分支，随机伪 ZIP/错误 Hash 在 E2E 返回 400 并标记 FAILED。线上对象存储读取尚未接入。
- [x] ★ **08-22** 预签名 URL 设置短过期时间，Bucket 默认私有。
  完成证据（2026-08-29）：MinIO 预签名 URL 默认 900 秒且服务端上限 7 天；匿名 GET 私有 bucket 返回 403，预签名接口只允许会话所有者访问。
- [x] ★ **08-22** 文件去重不能让 A 用户借由 Hash 获取 B 用户文件访问权。
  完成证据（2026-08-29）：`FileObject` 查询始终带 `ownerId`，跨用户使用同一 SHA-256 只会创建自己的 UploadSession，不返回对方 `fileObjectId`；E2E 已验证 A/B 隔离。
- [ ] ★ **08-23** Worker 从对象存储流式计算 SHA-256，核对客户端指纹。
  状态：已实现并通过本地 MinIO ImportJob E2E；`GetObject` 响应流由 `readObjectWithSha256` 增量计算摘要，再与 `FileObject.sha256` 比对后解析 JSON。该星标任务仍待用户本人完成理解验收后勾选。
- [x] ★ **08-23** ZIP 增加压缩炸弹、文件数量和解压后体积限制。
  完成证据（2026-08-29）：本地 ZIP 流式扫描拒绝路径穿越、绝对路径、数据描述符、超过 1000 个条目或 100 MiB 解压体积；E2E 分别构造路径穿越、1001 条目和 100 MiB+1 条目，均返回 400 并将会话标记为 FAILED。
- [x] **08-23** 对过期、失败的 multipart upload 定时终止并清理元数据。
  完成证据（2026-08-29）：`UploadsService` 启动时执行清理并每小时扫描过期会话；本地 MinIO E2E 将会话改为过期后调用清理器，状态变为 `EXPIRED`，远端 Multipart 被 Abort，未把清理失败伪装成完成。报告：`docs/evidence/object-storage-report.md`。

### 暂停、恢复和断点续传

- [x] **08-23** 每个进行中的分片绑定 `AbortController`。
  完成证据：`uploadFile` 将同一个 `AbortSignal` 传给每个分片请求和重试等待。
- [x] ★ **08-23** 暂停时停止调度新任务并取消正在传输的分片。
  完成证据（2026-08-29）：`runWithConcurrency` 感知 `AbortSignal`，暂停后不再调度等待任务；`UploadPanel` 调用同一控制器取消进行中的分片；单测验证中止后启动任务数不超过并发上限。
- [x] ★ **08-23** 将 UploadSession、文件指纹和已完成 part 状态保存到 IndexedDB。
  完成证据（2026-08-29）：Chromium 中开始上传并完成首片后暂停、关闭页面，再在同一浏览器上下文新建页面并重新选择文件；IndexedDB 恢复 sessionId/partSize/sha256/已完成分片，网络拦截确认恢复阶段没有重传第 1 片。无 IndexedDB 时的内存降级另有单测。
- [x] ★ **08-23** 恢复时向服务端查询已完成分片，只上传缺失部分。
  完成证据（2026-08-29）：恢复流程先请求 `GET /uploads/:id`，根据服务端已完成 part 跳过已有分片；单测验证 3 片中已有 1 片时只发送其余 2 片。
- [x] **08-23** 文件重新选择后验证 name、size、lastModified 和 sha256 是否匹配原会话。
  完成证据（2026-08-29）：恢复键包含文件名、大小和修改时间，进度记录增加 SHA-256；同名同大小但指纹不同的文件不会复用旧会话，单测已覆盖。
- [x] **08-23** 页面刷新、浏览器重启和 Electron 重启后均可继续。
  完成证据（2026-08-29）：Playwright 分别验证同一上下文关闭页面恢复、固定 profile 关闭整个 Chromium 进程后恢复，以及关闭整个 Electron 进程后恢复；三种场景都复用原 sessionId，核对服务端缺片，只补传未完成分片并最终完成 ImportJob。完整套件 6/6，报告见 `docs/evidence/upload-recovery-report.md`。

### 秒传设计

- [x] ★ **08-23** 相同用户重复上传同一 SHA-256，可复用已有 FileObject 引用。
  完成证据（2026-08-29）：`FileObject` 使用 `(ownerId, sha256)` 唯一约束；同一用户第二次 initiate 直接返回已验证对象，重复 complete 也复用同一 ID，E2E 已验证数据库只保留一个对象。
- [x] ★ **08-23** 跨用户物理去重只在服务端内部执行，不向客户端泄露文件是否存在。
  完成证据（2026-08-29）：完成时先校验完整 SHA-256，再由 `UploadsService` 仅在服务端查找同后端、同 bucket、同大小的已验证对象；用户 B 获得独立 `FileObject` 所有权记录但复用 A 的物理键，重复字节会删除。E2E 验证 B 的 initiate 不返回 A 的对象 ID，完成响应不含 objectKey/ownerId，A 仍不能读取 B 会话，B 的 ImportJob 能读取受控共享字节。
- [x] ★ **08-23** “秒传”只是复用已验证文件，不跳过权限、配额和所有权记录。
  完成证据（2026-08-29）：秒传分支只匹配当前 `ownerId` 且要求 `verifiedAt`，仍经过登录与配额边界；跨用户相同 Hash 不泄露对象 ID，E2E 已覆盖。

### 测试场景

- [x] **08-23** 上传 100 MiB 测试文件并验证 SHA-256 一致。
  完成证据（2026-08-29）：后端 E2E 构造 100 MiB JSON 文件，按 8 MiB 分片上传 13 片，服务端流式计算 SHA-256 与客户端摘要一致，文件大小一致；该用例随完整 E2E 重复运行通过。
- [x] **08-23** 37% 时暂停，刷新页面后继续并最终完成。
  完成证据（2026-08-29）：Playwright 对 `40 MiB + 101 bytes` 文件限制上传吞吐，并用 MutationObserver 在页面进度文本首次等于 37% 时立即点击暂停；测试核对 IndexedDB/服务端完成片，关闭整个 Chromium 后恢复，断言已完成片不重传、缺片不重复，最终导入成功。
- [x] **08-23** 模拟一个分片连续两次失败，验证退避重试。
  完成证据（2026-08-29）：前端单测让上传任务连续返回两次 503 后成功，断言共尝试 3 次；`withUploadRetry` 使用指数退避并限制最大重试次数，测试可重复运行。
- [x] **08-23** 篡改一个分片，最终完整性校验必须失败。
  完成证据（2026-08-29）：后端 E2E 声明全文件 SHA-256 后上传不同内容，合并时返回 400 并将 UploadSession 标记为 FAILED；错误状态可再次查询确认。
- [x] **08-23** 同一文件连续选择三次，只保留一份有效对象和三条合法业务引用或一条幂等任务。
  完成证据（2026-08-29）：后端 E2E 同一用户连续三次 initiate/complete 相同 SHA-256，三次返回同一 FileObject ID，数据库对象计数为 1；三次 ImportJob 幂等提交另有用例验证只保留一个 jobId。
- [x] **08-23** 两个用户使用同一文件时不能互相访问对象。
  完成证据（2026-08-29）：后端 E2E 用户 A/B 使用相同 SHA-256，B 创建自己的会话且不获得 A 的 fileObjectId，A 读取 B 会话返回 404；完成/状态接口均按 ownerId 隔离。
- [x] **08-23** 测量并发数 1、3、6、10 下的耗时、失败率、内存和主线程卡顿。
  完成证据（2026-08-29）：可重复脚本固定 96 MiB、12 片、8 MiB/片、本机环回和 60 ms 单片延迟；最近一次并发 1/3/6/10 为 1003.6/359.3/222.9/233.4 ms，正常场景最终失败率均为 0，配置 10 时实际连接峰值仍为 6。另注入第 4 片首次 503，共 13 次尝试、1 次尝试失败、最终 0 片失败；报告同时保存 Chromium JS 堆与 Long Task，并明确不可外推生产。

### 大文件模块的面试追问

- [ ] ★ **08-23** 为什么选择对象存储，而不是 PostgreSQL 或 Render 本地磁盘？
  状态：待用户本人完成理解验收；代码仍是本地文件演示，不能用规划答案代替对象存储实战。
- [x] **08-23** 为什么选 8 MiB，分片过大或过小分别有什么问题？
  完成证据（2026-08-29）：基准实际把 96 MiB 文件切为 12 个 8 MiB 分片，并保存权衡：更小可细化恢复粒度但增加请求、元数据和签名开销；更大减少请求但增加失败重传成本、单片内存和进度跳跃。8 MiB 是当前本地基线，不宣称为所有网络或对象存储的最优值。
- [ ] ★ **08-23** 为什么不能直接 `Promise.all(chunks)`？
  状态：`runWithConcurrency` 代码、峰值单测和 1/3/6/10 报告已存在；待用户本人说明全量请求的资源风险和有界调度权衡后手动勾选。
- [ ] ★ **08-23** Web Worker 解决 CPU 阻塞还是网络并发？
  状态：真实 Worker Hash 与 Long Task 对比已验证；待用户本人说明它隔离 CPU 计算而不负责网络并发后手动勾选。
- [ ] ★ **08-23** 如何证明断点续传不会漏片或重复片？
  状态：单测和完整 Chromium/Electron 进程 Playwright 已验证本地记录、服务端完成片、请求拦截和最终合并四段证据；待用户本人完成证据链讲解后手动勾选。
- [ ] ★ **08-23** 客户端 Hash 可以伪造，服务端怎样最终验证？
  状态：本地服务端流式 Hash 与篡改失败 E2E 已存在；待用户本人讲清客户端指纹不可信、服务端最终校验边界后手动勾选，对象存储 Worker 版仍未实现。
- [ ] ★ **08-23** 秒传会不会泄露别人上传过某个文件？
  状态：跨用户相同 Hash 不返回对方对象 ID 的 E2E 已存在；待用户本人解释所有权查询与物理去重的边界后手动勾选。
- [x] **08-23** S3 multipart ETag 为什么不一定等于文件 MD5？
  完成证据（2026-08-29）：`npm run test:minio:etag` 用本地 MinIO 真实完成两个分片；整文件 MD5 为 `94394a093d0b2ee3f390c7c97cc9fd69`，Multipart ETag 为 `87ba9c9d2e69480fe31b834308ef08dc-2`，后者由分片 MD5 的二进制拼接再计算并附带 `-2`。这不是线上 S3 兼容性宣称。

---

## 第二阶段-2：上传后的异步导入流水线

> 大文件“传完”不等于题库“导入完成”。传输与业务处理必须分开。

- [x] **08-23** Redis + BullMQ 建立 ImportJob 队列。
  完成证据（2026-08-29）：配置 `REDIS_URL` 时 API 使用 BullMQ 持久队列入队，ImportJob 数据库记录与 Bull job 共用 jobId；独立测试库加本地 Redis `127.0.0.1:6380` 的 Worker 集成测试 5/5，验证任务进入 completed、幂等任务复用和真实队列深度。未配置 Redis 时保留单进程本地降级，不宣称线上 Redis 已部署。
- [x] ★ **08-23** 上传校验完成后创建 ImportJob，不在上传请求里执行 OCR/解析。
  完成证据（2026-08-29）：UploadPanel 只在 complete 返回已验证 FileObject 后调用 `POST /import-jobs`；Chromium Playwright 验证上传完成后自动创建任务、后台 Worker 导入并显示 `SUCCEEDED 1/1`，上传接口本身不执行题目解析。
- [x] ★ **08-23** 状态：QUEUED、PARSING、VALIDATING、DEDUPING、WAITING_REVIEW、PUBLISHING、SUCCEEDED、PARTIAL、FAILED、CANCELLED。
  完成证据（2026-08-29）：`review-*` ImportJob 在确定性校验后进入 `WAITING_REVIEW`，管理员发布时经过 `PUBLISHING` 并最终 `SUCCEEDED`；编辑者越权发布返回 403。证据：`docs/evidence/import-review-and-frontend-quality-report.md`，审核发布 E2E 通过。
- [x] ★ **08-23** 使用 `(userId, fileSha256, pipelineVersion)` 生成幂等键。
  完成证据（2026-08-29）：未显式传入幂等键时，服务端使用文件已验证 SHA-256 与 `pipelineVersion` 生成 key，并通过 `(userId, idempotencyKey)` 唯一约束落库；E2E 已验证重复请求复用同一任务。
- [x] ★ **08-23** 同一文件连续触发三次只运行一个任务，并返回原 jobId。
  完成证据（2026-08-29）：同一用户对同一文件连续发送 3 次 `POST /import-jobs`，三次响应返回同一个 jobId，数据库只保留一条任务记录。
- [x] ★ **08-23** Worker 设置合理并发数，防止 OCR/AI 请求打满内存或外部额度。
  完成证据（2026-08-29）：独立 Worker 默认 `IMPORT_WORKER_CONCURRENCY=2`，可通过环境变量调整；BullMQ 集成测试在真实本地 Redis 上启动独立 `dist/src/worker.js` 并完成任务，构建和 5/5 Worker 测试通过。当前 JSON 解析不调用付费 OCR/AI。
- [x] ★ **08-23** 瞬时错误指数退避；格式错误进入失败报告，不做无意义重试。
  完成证据（2026-08-29）：BullMQ 配置 3 次 attempts 和指数退避；集成测试先移走源文件制造瞬时失败，观察 `Attempt 1/3` 后恢复文件并成功；schemaVersion 错误由 `PermanentImportError` 判为永久错误，只执行 1 次并保存逐项失败报告。
- [x] **08-23** 记录每个阶段的耗时、输入数、通过数、重复数和失败数。
  完成证据（2026-08-29）：ImportJob 持久化解析/校验起止时间和耗时，并记录 total/imported/skipped/failed；合法 JSON E2E 断言 1/1/0/0 计数及两个阶段耗时均为数字，非法 schemaVersion 断言失败原因。

> 本地验证证据（2026-08-29）：后端 E2E 62/62；合法 JSON 进入 `SUCCEEDED`，非法 schemaVersion 进入 `FAILED`，PARTIAL 保留逐项报告，其他用户读取任务/对象返回 404。配置 Redis 时由 BullMQ 独立 Worker 执行，Worker 集成测试 5/5；不配置 Redis 时仍可用进程内学习降级。
- [x] ★ **08-24** 前端使用 SSE 接收单向任务进度，断线后携带 Last-Event-ID 恢复。
  完成证据（2026-08-29）：前端解析 `text/event-stream` 并保存最后事件 ID；Playwright 注入事件 ID 7、强制下一次事件请求断网，再断言重连请求携带 `Last-Event-ID: 7` 且最终恢复到 `SUCCEEDED`，过期的断线错误提示被清除。
- [x] **08-24** 提供错误行/文件报告和重新处理失败项的入口。
  完成证据（2026-08-29）：`ImportJob.failureReport` 持久化逐项序号与失败原因；失败任务和 PARTIAL 任务由 `UploadPanel` 提供 JSON 下载和重新处理入口。E2E 验证非法 schema 文件报告和 PARTIAL 的第 2 行失败报告；前端/后端构建通过。
- [x] ★ **08-24** Worker 被终止后，任务能够重试或由 watchdog 标记超时。
  完成证据（2026-08-29）：Worker 集成测试在 BullMQ job 进入 active 后强制 `SIGKILL` 进程，随后启动新 Worker；BullMQ stalled 检测重新领取并完成同一 jobId，数据库最终 `SUCCEEDED` 且队列状态 `completed`。无 Redis 的降级模式仍会在启动时恢复 QUEUED 或标记超时任务。

---

## 第三阶段-1：并发、幂等和冲突总表

| 场景 | 不能只靠什么 | 推荐方案 | 最终兜底 |
| --- | --- | --- | --- |
| ★ 用户连续点提交 | 禁用按钮/防抖 | clientRequestId | 数据库联合唯一约束 |
| ★ 多请求同时 Token 过期 | 每个请求各自刷新 | refresh single-flight + 等待队列 | RefreshSession 轮换/撤销 |
| ★ 相同题目并发导入 | 前端标题查重 | normalizedTitle | 数据库唯一索引 |
| ★ 同文件反复上传 | 文件名判断 | SHA-256 + UploadSession | FileObject 唯一约束/引用 |
| ★ 同导入任务反复触发 | 页面 loading | ImportJob idempotencyKey | 数据库唯一约束 |
| ★ 两人同时编辑题目 | 最后写入覆盖 | version 乐观锁 | 409 + 人工合并 |
| ★ 分片请求过多 | Promise.all | 并发池 | 服务端限流/对象存储限制 |
| ★ 用户狂点搜索/快速切页 | 只做防抖 | AbortController + 最新请求标识 | 忽略过期响应 |
| 离线重试同步 | 时间戳猜测 | 客户端操作 UUID + outbox | 服务端幂等消费 |

### 不急着使用分布式锁

- [x] ★ **08-24** 优先使用唯一约束、事务、幂等键和乐观锁。
  完成证据（2026-08-29）：题目标题、个人练习和 ImportJob 使用数据库唯一约束，服务端更新使用 `version` 条件更新；并发 PATCH 一个成功、一个返回 `QUESTION_VERSION_CONFLICT` 409，前端识别冲突并刷新服务器版本。
- [x] **08-24** 只有跨进程共享资源且数据库约束无法表达时，才评估 Redis 分布式锁。
  完成证据（2026-08-29）：`docs/adr/0004-redis-lock-boundary.md` 与 12 路并发 ImportJob E2E 显示当前由唯一约束、事务、幂等键和乐观锁完成仲裁，最终只保留 1 条任务；因此没有为了简历引入空业务锁。
- [x] **08-24** 如使用锁，必须回答锁过期、续租、进程崩溃和 fencing token 问题。
  完成证据（2026-08-29）：项目当前没有业务分布式锁；ADR 已把未来真实需求下的 TTL、owner token、续租上限、崩溃释放、fencing token、Redis 故障和网络分区测试要求写成引入前置条件，未把 BullMQ 内部 job lock 误作业务互斥锁。

---

## 第三阶段-2：题目协作治理

- [x] **08-24** Question 增加 DRAFT、IN_REVIEW、PUBLISHED、REJECTED、ARCHIVED 状态。
  完成证据（2026-08-29）：新增 `QuestionStatus` 枚举和默认 `DRAFT` 字段，迁移与 Prisma validate 通过，新增题目 E2E 验证返回 `DRAFT`。
- [x] **08-24** 编辑者提交草稿，管理员审核后发布。
  完成证据（2026-08-29）：编辑者创建题目默认为 DRAFT，可迁移到 IN_REVIEW；编辑者发布返回 403，管理员发布返回 PUBLISHED；E2E 同时查询修订和审计记录。
- [x] ★ **08-24** 状态迁移在 Service 中校验，不能任意 PATCH status。
  完成证据（2026-08-29）：`QuestionsService.transitionStatus` 使用白名单和角色判断，`UpdateQuestionDto` 不声明 status；E2E 直接 PATCH status 返回 400，合法迁移和越权发布分别验证 201/403。
- [x] ★ **08-24** QuestionRevision 保存前后快照、编辑者、原因和时间。
  完成证据（2026-08-29）：题目更新与版本检查、QuestionRevision、AuditLog 位于同一 Prisma transaction；E2E 验证快照 version 1 -> 2，冲突事务不会留下修订。
- [x] ★ **08-24** AuditLog 记录发布、删除、角色修改等关键操作。
  完成证据（2026-08-29）：新增管理员 `PATCH /users/:id/role` 和只读 `GET /audit-logs`；角色变更与题目发布、删除、回滚、更新均在数据库事务中写入 `AuditLog`。编辑者越权返回 403，非法角色返回 400，管理员成功变更后可读取 `USER_ROLE_CHANGED` 记录；后端 E2E 50/50、NestJS 构建通过。
- [x] ★ **08-24** Question version 乐观锁防止并发覆盖。
  完成证据（2026-08-29）：E2E 并发修改同一题目验证一个 200、一个 409，数据库最终 version 只递增一次；旧版本不会覆盖新版本。
- [x] **08-24** 409 页面展示“我的版本”和“服务器版本”。
  完成证据（2026-08-29）：Playwright 先在页面打开旧版本编辑表单，再用第二 API 会话更新服务器版本，提交旧 version 后真实触发 409；冲突面板同时显示未提交的本地答案和服务器答案，并提供载入服务器版本/保留本地修改两个入口。
- [x] ★ **08-24** 已发布题目可回滚到指定历史版本。
  完成证据（2026-08-29）：管理员携带当前 version 回滚指定 QuestionRevision，错误版本返回 409；成功回滚会递增 version，并在同一事务写入新修订和审计，E2E 已验证。

---

## 第三阶段-3：前端性能与工程化

- [x] ★ **08-24** 上传器使用状态机组织 hashing、uploading、paused、verifying、processing、done、failed。
  完成证据（2026-08-29）：新增 `uploadState.js` 定义九个状态与允许迁移，UploadPanel 的 Hash、上传、暂停、校验、导入、完成、失败、取消均经状态机；前端单测覆盖正常链路、暂停恢复、失败重试和非法跳转，Web/Electron 构建通过。
- [x] ★ **08-24** Web 与 Electron 共用上传业务逻辑，通过 Transport 适配平台差异。
  完成证据（2026-08-29）：两端共用 `uploadFile` 的 Hash、切片、并发、重试、IndexedDB 和恢复流程，只由 `createUploadTransport` 选择 Web XHR 或 Electron 主进程 `apiFetch` 桥；Transport/失败重试单测、Web Playwright、Web/Electron 构建均通过。Electron IPC 字节级进度和重启恢复仍作为独立未完成项保留。
- [x] **08-24** 路由组件改为动态 import，按页面拆包。
  完成证据（2026-08-29）：Web 路由使用异步组件，Web/Electron 构建产物均生成 Home、Practice、Stats、Questions 等页面 chunk。
- [x] **08-24** 题目数量上千后改用服务端分页、筛选和排序。
  完成证据（2026-08-29）：`FindQuestionsDto` 和 `QuestionsService.findAll` 支持 page/pageSize、keyword/category/tag/status，服务端固定 id 升序保证分页稳定；E2E 验证第二页和组合筛选结果。
- [x] **08-24** 搜索输入防抖，并取消上一条未完成请求。
  完成证据（2026-08-29）：`QuestionsPage` 使用 250ms debounce 合并输入，`questionsStore.loadQuestions` 在新请求前 abort 旧请求；前端单测验证只执行最后一次搜索，apiFetch 单测验证取消路径。
- [x] **08-24** 长列表达到实际卡顿阈值后引入虚拟列表，不提前优化 225 条数据。
  完成证据（2026-08-29）：当前约 225 条并由服务端分页，未观察到实际卡顿阈值；保留分页/筛选并明确不提前引入虚拟列表。该结论和性能边界记录于 `docs/evidence/import-review-and-frontend-quality-report.md`。
- [x] **08-24** Pinia Store 区分 idle、loading、success、error 状态。
  完成证据（2026-08-29）：questions Store 在请求开始/成功/失败/取消分别维护 status 和 error；前端单测覆盖服务端成功状态与失败后本地回退状态。
- [x] **08-24** 空状态、错误状态、重试状态和权限不足状态完整呈现。
  完成证据（2026-08-29）：Playwright 使用独立 LEARNER 测试账号，在真实 Chromium 依次验证带 requestId 的 503 错误提示、点击重试后空题库、恢复正常数据、搜索无匹配，以及批量导入禁用、编辑按钮隐藏和权限说明。测试数据只写 `TEST_DATABASE_URL` 并在结束后清理；完整 Playwright 6/6 通过。
- [x] ★ **08-24** 建立共享 `apiFetch` 请求层，不在组件散落裸请求。
  完成证据（2026-08-29）：题目、练习、上传、ImportJob 请求统一经过 `apiFetch`，集中处理认证头、Cookie、超时、Abort、401 single-flight 和错误码；仅保留登录/刷新原始 fetch 与静态示例 JSON fetch。前端 81/81、Web/Electron 构建通过。
- [x] **08-24** 使用性能面板记录大文件 Hash 前后主线程 Long Task 数量。
  完成证据（2026-08-29）：自动化基准通过 Chromium `PerformanceObserver` 记录 50 ms 阈值 Long Task；最近一次主线程增量 SHA-256 为 3 个、总 169 ms、最长 68 ms，Worker 方案观察到 0 个，两者摘要一致。报告明确“本次未观察到”不等于零卡顿或所有设备都无 Long Task。

---

## 第四阶段-1：可观测性与安全

### 日志与错误追踪

- [x] **08-24** NestJS 使用 Pino 结构化日志。
  完成证据（2026-08-29）：引入 `pino`，请求 middleware 输出 JSON 的 requestId、route、method、status、duration，并按请求上下文附带 userId/jobId；完整 E2E 输出已观察到正常、4xx 和 409 结构化记录，NestJS 构建通过。
- [x] ★ **08-24** requestId 贯穿 API、Worker、ImportJob 和前端错误。
  完成证据（2026-08-29）：API 中间件生成/回传 requestId，ImportJob 持久化创建请求 ID，BullMQ payload 传给独立 Worker，Worker 的阶段/重试/失败日志继续携带同一 ID，前端 `ApiError` 和页面错误区显示 requestId；Worker 集成测试断言进程输出同时包含 jobId 和创建请求 ID。
- [x] ★ **08-24** 日志包含 route、status、duration、userId、jobId，但不记录密码和 Token。
  完成证据（2026-08-29）：Pino 请求 middleware 和 ImportJob logger 均输出 JSON；后端 E2E 日志实际观察到 route、status、durationMs、userId、jobId 和同一 requestId，登录/上传/导入日志未输出密码或 Token；62/62 E2E、NestJS 构建通过。
- [x] **08-24** Web、Electron renderer 和 API 接入 Sentry 或本地等价错误记录。
  完成证据（2026-08-29）：Web/Electron 共用 `TelemetrySettings.vue` 和本地错误/行为记录；API 使用 Pino JSON 日志。默认关闭且递归移除答案、Token、密码等敏感字段。外部 Sentry 未配置，不宣称远程告警。
- [x] **08-24** Sentry 或本地等价错误记录关联 commit/release，支持定位某次发布引入的错误。
  完成证据（2026-08-29）：Vite 以 `package.json` 版本与当前 Git 短 SHA 注入 `VITE_APP_RELEASE`；本地错误事件保存 release、route 和可用的 requestId，API Pino 使用同一 requestId 关联请求。未上传 Sentry release。

### 健康与指标

- [x] **08-24** `/health/live` 检查进程。
  完成证据（2026-08-29）：`GET /health/live` 不访问数据库，仅返回进程服务状态；E2E 验证 200 和 `status: ok`，并与 `/health/ready` 的数据库检查区分。
- [x] ★ **08-24** `/health/ready` 检查 PostgreSQL、Redis 和对象存储。
  完成证据（2026-08-29）：readiness 执行 PostgreSQL `SELECT 1`、检查当前本地文件存储目录读写，并在配置 Redis 时执行真实 PING/队列深度读取；E2E 验证正常 200、本地目录不可用返回 503，Worker 集成测试验证 Redis 正常为 `ok`、断开时返回 `REDIS_NOT_READY` 503。这里的对象存储仅指当前本地文件后端，不代表 MinIO/S3/R2 或生产持久化已验证。
- [x] **08-24** 统计请求量、5xx、p95、队列深度、任务耗时、失败率和同步冲突数。
  完成证据（2026-08-29）：新增 `/health/metrics` 和 `MetricsService`，采样请求量/5xx/p95、ImportJob 完成与失败率/耗时、进程内队列深度和 409 冲突数；health E2E 验证端点字段与正常响应。当前为进程内 1000 条滑动窗口，不代表多实例长期指标。
- [x] **08-24** 建立告警，但阈值必须来自真实基线。
  完成证据（2026-08-29）：`scripts/check-alert-baseline.mjs` 从真实上传基准报告读取并计算 p95，生成可重复的 2x p95 告警阈值；脚本输出 p95=1003.6ms、阈值=2008ms。当前为本地基线规则，未宣称线上告警渠道。

### 安全防线

- [x] ★ **08-24** 登录和上传初始化接口限流。
  完成证据（2026-08-29）：`configureApp` 对登录、注册、上传初始化按 IP/接口使用每分钟 30 次进程内桶；E2E 第 31 次登录返回 429 和 `Retry-After: 60`。多实例共享限流仍需 Redis。
- [x] **08-24** Helmet、安全响应头和严格 CORS 白名单。
  完成证据（2026-08-29）：引入 Helmet 并保留显式 CORS origin 白名单；E2E 验证 `nosniff`、`SAMEORIGIN`、允许来源和恶意来源不返回授权头。
- [x] ★ **08-24** 上传文件类型、大小、数量和用户配额限制。
  完成证据（2026-08-29）：服务端限制 MIME、单文件 500 MiB、每用户 100 个文件和 2 GiB 已占用/进行中空间；E2E 验证 MIME、累计字节配额和跨用户归属。配额为本地单进程实现，多实例集中计数仍未接入。
- [x] ★ **08-24** 对压缩包防止 Zip Bomb 和路径穿越。
  完成证据（2026-08-29）：本地合并文件流式扫描拒绝路径穿越、绝对路径、1001 个条目和 100 MiB+1 解压体积；E2E 全部验证失败并清理为 FAILED。线上对象存储演练尚未进行。
- [ ] ★ **08-24** 预签名 URL 最小权限、短过期、对象 key 不可预测。
  完成证据（2026-08-29）：本地 MinIO 适配器只签名单个 `UploadPart`，默认 900 秒且服务端上限 7 天；Bucket 匿名 GET 返回 403，对象键为用户 ID 加随机 UUID，预签名接口强制会话所有权和分片范围校验。生产 S3/R2 策略仍未验证。
- [x] ★ **08-24** 管理操作审计，不允许普通日志替代审计日志。
  完成证据（2026-08-29）：角色修改、发布、删除、回滚、题目更新分别写入 `AuditLog`，并与业务更新处于同一 Prisma transaction；管理员审计查询和编辑者越权 E2E 通过。Pino 请求日志仅做运行排查，不替代数据库审计记录。
- [ ] **08-25** Dependabot/CodeQL 或等价依赖与代码扫描。
  状态：已配置待 GitHub Actions 验证。新增 `.github/dependabot.yml` 和 `.github/workflows/codeql.yml`；前端 `npm audit` 复扫为 0 个漏洞，后端 Prisma 7 链路仍有 3 个 high，且本轮没有 GitHub Security/Dependabot 线上记录，仍保留未勾选。证据：`docs/evidence/ci-quality-gate-report.md`。

### 故障演练

- [x] ★ **08-25** Redis 断开时，普通刷题仍能使用，导入功能明确降级。
  完成证据（2026-08-29）：Worker 集成测试将 Redis 指向不可连接的 `127.0.0.1:6399`；`GET /questions` 仍返回 200，readiness 返回 `REDIS_NOT_READY` 503，创建 ImportJob 返回 `IMPORT_QUEUE_UNAVAILABLE` 503，故障不会拖垮普通题库请求。
- [x] ★ **08-25** Worker 中途退出时，任务不丢失。
  完成证据（2026-08-29）：真实 BullMQ 任务 active 后强制终止 Worker，新 Worker 通过 stalled 恢复同一 jobId 并成功导入，队列最终 completed；测试使用独立 PostgreSQL 测试库和本地 Redis，可重复运行。
- [x] ★ **08-25** PostgreSQL 不可用时 readiness 失败并触发告警。
  完成证据（2026-08-29）：`HealthController` 对 Prisma `SELECT 1` 的真实失败调用本地 `ReadinessAlertService`，并返回 `503 POSTGRES_NOT_READY`；E2E 注入 `simulated postgres outage`，断言响应和 Pino/内存告警快照都包含 `postgres`。这是本地等价告警，不宣称已接入 PagerDuty、Sentry 或线上告警平台。
- [x] ★ **08-25** 对象存储超时时，分片有限重试并可继续。
  完成证据（2026-08-29）：MinIO E2E 使用本地一次性延迟代理将首个 UploadPart 延迟 250 ms，同时将 `S3_TIMEOUT_MS` 设为 50 ms；首次 API 返回 500 且 `uploadedParts` 仍为空，重试同一 session 的同一分片返回 201，complete 后 SHA-256 一致。前端 `withUploadRetry` 对 5xx 使用最多两次指数退避；本地 MinIO 验证不代表生产 S3/R2 故障演练。
- [x] **08-25** 前端制造异常后，Sentry 或本地等价错误记录能看到路由、版本和 requestId。
  完成证据（2026-08-29）：Vue errorHandler、window error 和 unhandledrejection 都调用 `recordClientError`；前端单测验证 route/requestId 被保存且答案正文与嵌套敏感字段被剔除。记录仅保存在用户启用后的本地存储。

---

## 第四阶段-2：完整 CI/CD

- [x] **08-25** PR 执行 format check、lint、前端单测、后端单测。
  完成证据（2026-08-29）：CI YAML 已配置四项门禁；本地 `format:check`、前后端 lint、前端测试 81/81、后端单测 12/12 均通过。GitHub Actions 线上成功仍未宣称。
- [ ] **08-25** Actions 使用 PostgreSQL、Redis、MinIO service containers 跑集成测试。
  状态：CI 已声明 PostgreSQL、Redis、MinIO service containers，并配置 MinIO 对象往返/404 探针、E2E、Worker 和 Playwright 命令；本机无 Docker，且尚无 GitHub Actions 线上成功记录，继续保留未勾选。当前产品上传仍为本地文件系统，不能把 MinIO 容器探针写成生产对象存储接入。
- [x] **08-25** 执行 Prisma validate 和迁移测试。
  完成证据（2026-08-29）：CI 工作流在独立 PostgreSQL service container 执行 `prisma migrate deploy` 与 `prisma validate`；本地测试库已应用全部迁移并重复通过 E2E/validate。正式 Neon 仍仅只读检查。
- [x] **08-25** Playwright 覆盖登录、刷题、上传暂停恢复、导入进度的冒烟流程。
  完成证据（2026-08-29）：Playwright 6/6 通过；Chromium 主流程覆盖登录、真实 Worker Hash、XHR 字节进度、暂停、关闭页面、IndexedDB 恢复、只补传缺片、自动 ImportJob、SSE 断线恢复、刷新后刷题；独立进程用例在 37% 暂停后关闭整个 Chromium 并复用 profile 恢复。其余用例覆盖真实 409 冲突面板、Web 写入/Electron 读取与会话重启、空/错/重试/无匹配/权限不足状态，以及 Electron IPC 字节进度与整个桌面进程重启后的分片恢复和最终导入。
- [x] **08-25** 构建 Web、NestJS、Worker 和 Electron。
  完成证据（2026-08-29）：`npm run build`、`npm run desktop:build`、`server/npm run build` 均通过；NestJS 构建产出 `dist/src/main.js` 和独立 `dist/src/worker.js`，Worker 集成测试直接启动该产物并通过 5/5。
- [ ] **08-25** main 自动部署 staging，并执行 smoke test。
  阻塞：缺少 staging 环境、部署凭据和经批准的远程发布操作；本轮未修改远程仓库。
- [ ] **08-25** tag 发布生产 API 和 Windows 安装包。
  阻塞：需要明确的生产发布确认；按约束未创建 tag、Release 或远程部署。
- [ ] **08-25** Release 附带 SHA-256 校验文件和 changelog。
  阻塞：没有经批准的新 Release；不能用本地构建结果冒充线上发布资产。
- [ ] **08-25** GitHub Environments 隔离 staging/production secrets。
  阻塞：需要修改远程仓库 Environments/Secrets，当前没有授权执行该外部变更。
- [ ] **08-25** 生产数据库使用 `prisma migrate deploy`。
  状态：阻塞：正式 Neon 仍有 5 个迁移待人工审查和确认（新增 `20260829130000`、`20260829140000`、`20260829150000`、`20260829160000`、`20260829170000_add_import_request_id`）。本轮曾误触发一次未显式指定测试连接的 deploy，输出显示尝试应用 `20260829100000`、`20260829120000` 后在 `20260829121000` 重复索引错误处停止；未继续修复或 resolve，后续必须先人工核对 `_prisma_migrations` 和实际表结构。
- [x] ★ **08-25** 数据库使用 expand-contract 迁移，避免应用回滚后无法读取旧结构。
  完成证据（2026-08-29）：`server/scripts/expand-contract-smoke.mjs` 在独立 `TEST_DATABASE_URL` 上模拟旧客户端仅写既有必填字段；新结构读取结果为 `version=1`、`status=DRAFT`、`importJobId=null`，证明新增列的默认/可空策略兼容旧写入。正式数据库未迁移，删除或改名列仍须在所有实例升级后的独立 contract 版本处理。
- [ ] ★ **08-25** 演练一次失败部署和应用版本回滚并记录过程。
  阻塞：缺少 staging/生产环境与批准的发布窗口；本地构建失败不等于部署回滚演练。

---

## 附录 A：未来简历只保留的四条主线

> 以下是目标结构，不代表目前已经完成。

1. ★ **多用户与权限**：安全会话、RBAC、对象级数据隔离、跨端学习记录同步。
2. ★ **大文件可靠传输**：对象存储 Multipart Upload、Web Worker Hash、Promise 并发池、断点续传和完整性校验。
3. ★ **异步任务与并发治理**：BullMQ 状态机、幂等键、有限重试、数据库唯一约束和乐观锁。
4. ★ **可观测与交付**：requestId、日志/指标/告警、故障演练、集成/E2E 测试和 CI/CD。

## 附录 B：每项功能写进简历前的门槛

- [ ] **08-25** 我能画出完整数据流。
  状态：必须由用户本人完成口述或画图验收，自动化测试不能代替理解证明。
- [ ] **08-25** 我能解释为什么需要它，而不是只会背定义。
  状态：必须由用户本人结合业务场景完成理解验收。
- [ ] **08-25** 我亲手写过最关键的 10 到 30 行核心逻辑。
  状态：本轮为全量快速实现模式，代码主要由 Codex 完成；不能虚构为用户亲手编写。
- [ ] **08-25** 我见过它在页面或数据库中的前后变化。
  状态：已有 Playwright、HTTP 和数据库测试证据，仍待用户本人查看并复述前后变化。
- [ ] **08-25** 我有测试、压测、日志或故障演练作为证据。
  状态：测试、Worker 日志和 Redis 故障证据已存在；缺上传压测、对象存储/数据库故障及用户本人验收。
- [ ] **08-25** 我能说出替代方案和当前权衡。
  状态：必须由用户本人完成面试口述验收。
- [ ] **08-25** 我知道它仍然解决不了什么。
  状态：边界已记录在 README 和简历笔记，待用户本人完成理解验收。

---

## 附录 C：当前基线和缺口

### 已经完成并验证

- [x] Vue 3 + Pinia + Vue Router 单页应用。
- [x] 分类/标签筛选、作答、自评、历史和统计闭环。
- [x] JSON schemaVersion 1 批量导入。
- [x] 前后端标题归一化和应用层查重。
- [x] NestJS + Prisma + Neon PostgreSQL 云端题库 CRUD。
- [x] Web 与 Electron 共用同一套 `src/`。
- [x] Electron contextIsolation、preload 白名单和 API origin 白名单。
- [x] localStorage 全量备份、恢复和 `%APPDATA%` 文件镜像。
- [x] 71 个前端核心逻辑测试。
- [x] GitHub Pages、Render API 和 NSIS Windows 安装包。

### 尚未完成，不能提前写进简历

> 本段只汇总缺口，不重复建立任务框；执行时间和完成状态以正文任务为准。

- MinIO/S3/R2 Multipart、预签名 URL 和生产对象存储。
- 第 2 包的 5 个星标面试理解题仍需项目作者本人完成讲解验收。
- 完整 ImportJob 审核/发布阶段和跨用户物理文件去重。
- Sentry、长期指标、真实告警基线和对象存储/PostgreSQL 故障演练。
- GitHub Actions 线上成功记录、staging smoke test、生产迁移和失败回滚。

---

## 附录 D：明确不做或暂缓

> 以下内容是 08-20 至 08-25 的全程约束，不属于待完成任务，因此不使用任务框。

- 不为了简历拆微服务。
- 不在没有性能证据时加 Redis 查询缓存。
- 不先上 Kubernetes、服务网格或复杂网关。
- 不用 WebSocket 代替所有通信；任务进度单向推送优先 SSE。
- 不自己实现密码学、JWT 或文件 Hash 算法。
- 不把大文件二进制写进 PostgreSQL。
- 不把对象存储设成公开 Bucket。
- 不把前端路由权限当成后端安全控制。
- 不宣称高并发、高可用、零卡顿或 100% 正确。
- 不照搬与题库无关的音频 FFT、AST 治理或金融监控功能。

---

## 附录 E：Gemini 简历记录提取与纠偏

### 值得吸收

- 大文件切片、断点续传、文件指纹和完整性校验。
- 手写 Promise 并发池，限制同时上传的分片数。
- 使用 Web Worker 把大文件 Hash 计算移出主线程。
- `AbortController` 取消请求和暂停上传。
- Access Token + Refresh Token、并发刷新单飞机制。
- 动态路由、按钮权限和服务端鉴权。
- 请求层统一处理 Token、错误、超时、取消和有限重试。
- SSE 展示异步任务进度。
- 埋点、前端错误监控、白屏监控和发布版本关联。
- 用 STAR 结构表达“问题、方案、权衡、结果”。

### 必须纠正

- **不能把别人的技术直接写进简历。** 必须在当前项目中真正实现、测试并能现场演示。
- **前端隐藏按钮不是安全权限。** 动态路由和 `v-permission` 只改善界面，最终授权必须由后端 Guard 和 Service 保证。
- **Token 不应长期放 localStorage。** XSS 可以读取它；Web 优先使用内存 access token 和 HttpOnly refresh cookie。
- **MD5 只适合作为普通去重指纹，不适合作为安全完整性依据。** 新实现优先选择增量 SHA-256。
- **Web Worker 不等于自动多线程。** 一个 Worker 是把计算移出主线程；多个 Worker 才可能并行，还要考虑内存和调度成本。
- **不能先 Hash 就宣称零等待。** 完整读取 1GB 文件本身需要时间；应显示 Hash 进度并说明先计算与边传边算的权衡。
- **不能把 Promise.all 用于几百个分片。** 它会一次创建大量请求，应使用有上限的并发池。
- **不能把 `try/catch` 称为高可用。** 高可用还需要重试边界、幂等、健康检查、监控、告警和恢复验证。
- **不能无压测宣称高并发、零卡顿或成功率 99%。** 数字必须来自可复现脚本和报告。
- **不要为了简历把现有 fetch 全面换成 Axios。** 当前已有跨 Web/Electron 的 `apiFetch`；应增强现有抽象。上传进度可单独使用 XHR 或上传 Transport。

### 本项目采用的业务主线

**用户把大量面试截图、PDF、ZIP 或 JSON 上传到系统，平台可靠传输文件，在后台提取和校验题目，进入人工审核后发布；用户的学习数据则在 Web 与 Electron 间同步。**

它让多用户、大文件上传、并发控制、对象存储、Redis 队列、AI/OCR、审核状态机、监控和 CI/CD 围绕同一业务问题展开，而不是互不相关的技术拼盘。

---

## 附录 F：08-20 至 08-25 全量冲刺时间索引

> [!important] 当前执行目标
> 在 2026-08-25 结束前，将登录权限、跨端个人数据、大文件分片与断点续传、Redis、Worker、SSE、协作治理、监控和 CI/CD 全部推进到最小可运行、可验证、可解释的闭环。正文每个任务框旁的日期是实际执行日期，本索引只负责按时间导航，不替代正文验收项。

### 08-20：测试地基与登录会话

- 第一阶段-1：查询 DTO、写入白名单、危险接口保护、独立 PostgreSQL 集成测试和 README 测试证据。
- 第一阶段-2：User、RefreshSession、密码哈希、Access Token、Web Cookie、注册/登录/刷新/退出测试。
- 当日验收：后端构建通过，Questions API 测试可重复运行，登录会话主链路能真实请求验证。

### 08-21：权限与个人数据

- Token 并发刷新、Electron `safeStorage`、RBAC、对象级权限和 401/403/越权测试。
- PracticeRecord、Favorite、Note、`clientRequestId` 幂等、本地迁移预览和 Web/Electron 数据同步。
- 当日验收：两个账号数据隔离，10 个过期请求只有 1 次 refresh，同一练习请求重放只新增 1 条。

### 08-22：大文件上传主链路

- UploadSession、UploadPart、FileObject、对象存储 Multipart 和预签名 URL。
- `Blob.slice()`、Web Worker SHA-256、XHR 上传进度、并发池、退避重试、完成合并和文件校验。
- 当日验收：真实文件完成切片上传与合并，进度可见，错误分片能够有限重试。

### 08-23：断点续传与后台任务

- `AbortController` 暂停、IndexedDB 保存进度、查询缺片恢复、Electron Transport、秒传与跨用户隔离。
- Redis、BullMQ、ImportJob 状态机、任务幂等、Worker 并发与重试。
- 当日验收：刷新后能继续上传；同一文件重复触发只运行一个任务；Worker 中断后任务可恢复或失败可见。

### 08-24：SSE、协作治理与监控安全

- SSE 进度与恢复、错误报告、审核状态机、QuestionRevision、AuditLog、乐观锁和 409 冲突处理。
- 前端请求治理、Pino、Sentry、健康检查、指标、告警、限流、Helmet、上传安全和权限审计。
- 当日验收：任务进度能实时显示；并发编辑不会静默覆盖；健康检查和错误追踪能看到真实依赖状态。

### 08-25：CI/CD 与最终验收

- 上传压测与面试追问、故障演练、依赖扫描、GitHub Actions、service containers、Prisma 迁移测试和 Playwright 冒烟测试。
- 构建 Web、NestJS、Worker、Electron；完成 staging smoke test、生产迁移、发布校验和失败回滚记录。
- 完成 README、演示流程、简历证据和完整数据流讲解。
- 当日验收：只有实际运行、测试或真实请求验证通过的正文任务才能勾选 `[x]`。

### 每天执行与容错

- 不要求上午起床。以起床为起点，吃饭洗漱后 60 分钟内开始第一块。
- 每天目标 10 个 25 分钟工作块，最低 6 个；分为起床后、晚饭前、晚饭后三组。
- 每完成 2 个工作块休息 15-25 分钟；完成最低线前手机留在房间外，不打开抖音和和平精英。
- 每天最后 1 个工作块只做构建、测试和证据记录；最晚 01:00 停止，不用通宵补进度。
- 当天未完成项顺延到次日前 2 个工作块，但不取消测试。8 月 25 日仍未通过验收的任务保留 `[ ]`。
- 08-26 开始 Agent 学习；题库项目转为处理验收缺陷，不继续无边界扩张。

---

## 08-26 之后的可选增强

> 以下内容统一排在 08-25 全量冲刺之后，不插入主线日期中间。

### AI/OCR 可选增强

- [x] **08-26+** 图片/PDF 提取结果只进入 Draft，不直接发布。
  完成证据（2026-08-29）：真实 PDF 文字层和预置英文语言包的 PNG OCR 均通过单测与 HTTP E2E；普通 `v1` 管线返回 400，`review-v1` 只生成 `DRAFT` 并停在 `WAITING_REVIEW`。无文字层 PDF 会失败而非生成假文本；OCR worker 每次完成后终止。当前只验证 `eng`，扫描 PDF OCR 和其他语言须另行预置并验证。证据：`docs/evidence/document-extraction-report.md`。
- [x] **08-26+** JSON Schema 做确定性字段校验。
  完成证据（2026-08-29）：ImportJob 使用 Ajv 严格 schema 校验 schemaVersion、题目字段、难度、标签和元数据范围，非法字段进入 FAILED 并保留原因。
- [x] **08-26+** 保存 source、模型版本、promptVersion、置信度和审核人。
  完成证据（2026-08-29）：ImportJob 持久化 source、promptVersion、confidence、reviewerId；本地 JSON 审核发布 E2E 验证 reviewerId 和发布审计。当前无 OCR/模型调用，不虚构模型或 token 数据。
- [x] **08-26+** 题目分类、难度和答案修订作为建议，由人工确认。
  完成证据（2026-08-29）：审核 Draft 持久化 `reviewSuggestions`，本地规则仅基于关键词和空白格式生成可解释的分类、难度和答案规范化建议；`UploadPanel` 展示理由，编辑者必须点击“采纳建议并保存”才复用带 `REVIEW_SUGGESTION_ACCEPTED` 原因的 Questions PATCH。HTTP E2E 验证审核预览返回建议、确认后写入 Revision、整批回滚仍可删除 Draft。该能力不是 AI/OCR 模型输出。
- [x] **08-26+** 语义去重只标记候选，不自动删除。
  完成证据（2026-08-29）：`semantic-candidates.ts` 使用本地 bigram/Jaccard 生成每题最多 3 个候选，持久化于 `ImportJob.duplicateCandidates`；单测验证输入不被修改，审核仍由人工决定，不依赖付费 embedding 服务。
- [ ] **08-26+** 记录 token、延迟、失败和费用。
  状态：已实现待验证。PDF/PNG 本地提取会持久化 provider、字符数、按字符数估算的文本 token、latency、failure 和 `cost: 0`；估算值不是模型实际计费 token。真实模型调用尚未有本地成功证据，因此不勾选。
- [ ] **08-26+** 核查公开题目来源和版权范围。
  状态：需要人工核查题目来源和素材授权，代码测试不能代替版权审查。

### 前端低优先级增强

- [x] **08-26+** Markdown 编辑器和预览。
  完成证据（2026-08-29）：题库管理答案字段支持编辑/预览切换，使用 `marked` 渲染 Markdown。
- [x] ★ **08-26+** 对用户输入的 Markdown/HTML 做 XSS 清洗。
  完成证据（2026-08-29）：统一通过 DOMPurify 白名单清洗，Node 测试覆盖 `<script>`、事件属性和危险标签攻击载荷。
- [x] **08-26+** 可访问性：键盘操作、焦点管理、ARIA、对比度。
  完成证据（2026-08-29）：答案编辑/预览使用可聚焦 tab 按钮、`role=tab` 与 `aria-selected`；全局表单和操作控件提供 `:focus-visible` 焦点样式；中性主题正文/控件颜色采用高对比度 token。完整辅助技术和 WCAG 自动扫描仍需后续补充。
- [x] **08-26+** 中性职业主题作为默认，个性主题作为可切换选项。
  完成证据（2026-08-29）：新增 Pinia theme store，默认中性职业主题并持久化切换专注主题；前端测试覆盖主题白名单和 localStorage 持久化。

### 四阶段完成后的产品增强

- [x] **08-26+** 基于遗忘曲线或 SM-2 的复习调度，并用历史记录解释推荐原因。
  完成证据（2026-08-29）：`reviewSchedule.js` 以 1/3/7/14/30 天间隔根据 correct/partial/wrong 计算 `reviewLevel` 与下次复习时间，`PracticeSession` 显示可解释推荐；前端单测覆盖提升与回退，不将其宣传为完整 SM-2。
- [ ] **08-26+** AI 模拟面试，使用 SSE 流式返回追问。
  状态：可选产品增强；不新增付费 AI 服务，当前没有模型与流式对话实现。
- [x] **08-26+** 答案评分拆成关键词覆盖、结构完整度和人工自评，不伪装成绝对正确。
  完成证据（2026-08-29）：`answerScoring.js` 分开计算关键词覆盖与列表结构完整度，`PracticeSession` 保留人工自评并明确“仅作复习提示，不代表绝对正确”；前端单测覆盖两个分数与提示语。
- [x] **08-26+** 题库导入预览、逐项修正和整批回滚。
  完成证据（2026-08-29）：审核 API 提供 preview、Questions PATCH 逐项修正和管理员 rollback；`UploadPanel` 增加 `review-v1`、预览、保存、发布与回滚操作。后端 E2E 验证修正/回滚/审计，Playwright 验证管理员上传 -> WAITING_REVIEW -> 修正 -> 发布 -> 刷新可见。
- [ ] **08-26+** Electron 自动更新、差分更新和签名。
  阻塞：需要代码签名证书、发布服务器和真实升级演练；当前仅有 NSIS 构建。
- [x] **08-26+** 用户行为埋点：筛选、开始作答、提交、自评、退出漏斗。
  完成证据（2026-08-29）：筛选、开始刷题和自评事件复用 `trackBehavior` 记录到用户主动启用后的本地存储；不保存答案正文。当前没有远程漏斗或用户量统计。
- [x] **08-26+** 隐私设置与遥测开关，默认不采集答案正文。
  完成证据（2026-08-29）：Web 和 Electron 共用 `TelemetrySettings.vue`，默认关闭；清洗器递归删除 answer/content/token/password/cookie 等敏感字段，前端单测覆盖顶层和嵌套字段。
